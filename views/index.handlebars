<h1>Burgers</h2>

<h2>Burgers that are cooked, but not devoured</h2>

<ul>
	{{#each burgers}}
		{{#unless cooked}}
			{{> burgers/burger-block cooked=true}}
		{{/unless}}
	{{/each}}
<ul>

<h2>Burgers that have been enjoyed</h2>

<ul>
	{{#each burgers}}
		{{#if cooked}}
			{{> burgers/burger-block cooked=false}}
		{{/if}}
	{{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

	<div class="form-group">
		<label for="bur">Burger Name:</label>
		<input type="text" id="bur" name="name">
	</div>

	<div class="form-group">
		<label for="cook">Cooked?</label><br>
		<input type="radio" name="cooked" value="1" checked>
		Cooked!<br>
		<input type="radio" name="cooked" value="0">Not Cooked Yet!
	</div>

	<div class="form-group">
		<label for="devour">Devoured?</label><br>
		<input type="radio" name="devoured" value="1" checked>
		Devoured!<br>
		<input type="radio" name="devoured" value="0">Not Eaten Yet!
	</div>

	<button type="submit">Add Burger</button>
</form>